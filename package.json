{
  "name": "component-library-test",
  "version": "0.0.0",
  "main": "dist/index.js",
  "private": false,
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "peerDependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "scripts": {
    "prebuild": "npm run clean",
    "build": "parcel build 'components/index.js' --no-cache",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@babel/preset-react": "^7.13.13",
    "@parcel/babel-plugin-transform-runtime": "^2.0.0-nightly.1823",
    "@parcel/babel-preset-env": "^2.0.0-beta.2",
    "parcel": "^2.0.0-beta.2"
  }
}
